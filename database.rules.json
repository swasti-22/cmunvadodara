{
  "rules": {
    "contacts": {
      ".read": false,
      "$contactId": {
        ".write": "!data.exists() && newData.exists()",
        ".validate": "newData.hasChildren(['name', 'email', 'type', 'message', 'timestamp'])"
      }
    }
  }
}
